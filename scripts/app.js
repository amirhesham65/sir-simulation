// Amir Hesham K.
// 2022


var toggleMobileMenu = () => {
    var mobileMenu = document.getElementById("mobile-menu");
    mobileMenu.className = (mobileMenu.className == "hidden") ? "block" : "hidden";
}